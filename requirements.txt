# ComfyUI-Qwen3VL-DP Dependencies
# Note: Qwen3-VL models require transformers >= 4.57.0

transformers>=4.57.0
torch>=2.0.0
huggingface-hub
modelscope
psutil
numpy
Pillow
opencv-python
bitsandbytes
accelerate

# Optional: For better performance
# flash-attn>=2.0.0  # Requires CUDA and compatible GPU (compute capability >= 8.0)

# Optional: For faster video loading
# qwen-vl-utils[decord]>=0.0.14